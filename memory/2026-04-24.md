
## Sequence Reordering with Radio Buttons

- Replaced the old "Edit Sequences" button + inline input with radio buttons per row in seating view
- `- Seq` / `+ Seq` buttons in the view toolbar (with Drag, +Rows, Del, 👁️ View)
- Radio buttons only show after clicking Edit (in seating view mode)
- Sequence swap logic: clicking + swaps current row's seats with next sequence up, - swaps down
- After swap, the radio follows the row so user can keep pressing the same button
- View mode label and edit header now show area description instead of code
- Added `selectedAreaDescription` ref
- Committed: `14b6c9f`

## Reminders
- Luke has a **small seat offset issue** to sort next time we speak (Stadium Maintenance page, mentioned after commit 7eeb878)
